specVersion: "v1"
serviceId: "opensignal"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "opensignal-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
